import 'package:flutter/material.dart';

class SahaDivide extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      height: 5,
      color: Colors.grey[200],
      child: Row(
        children: [
          Column()
        ],
      ),
    );
  }
}